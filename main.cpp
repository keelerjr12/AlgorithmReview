#include <iostream>
#include <vector>
#include "chp6.h"

int main() {

    //Keeler::print(std::begin(heap.Data()), std::end(heap.Data()));

    //Keeler::print(std::begin(heap.Data()), std::end(heap.Data()));

    return 0;
}
